# Copy to infra/.env and fill in.
#
# This file is read by docker compose via env_file in infra/docker-compose.yml.
# It is gitignored (infra/.env).

OPENAI_API_KEY=sk-your-key-here

# Optional: override model
OPENAI_MODEL=gpt-4o-mini

# Optional: OpenAI-compatible base URL (Azure/vLLM/etc)
# OPENAI_BASE_URL=https://your-host/v1

